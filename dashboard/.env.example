# Sesamum Dashboard - Environment Configuration Template
# Copy this file to .env.development or .env.production and configure as needed

# API Configuration
# Base URL for all API requests
VITE_API_BASE_URL=http://localhost:8000

# MSW (Mock Service Worker) Configuration
# Enable MSW to use mock data instead of real API
# true = use MSW (recommended for development)
# false = connect to real backend
VITE_ENABLE_MSW=false

# MSW Delay (milliseconds)
# Simulates network latency (only applies when MSW is enabled)
VITE_MSW_DELAY=800
